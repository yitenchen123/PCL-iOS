name: PCL-iOS Build

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]
  workflow_dispatch:

jobs:
  build:
    name: Build PCL-iOS
    runs-on: macos-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1
      
      - name: Show Xcode version
        run: xcodebuild -version
      
      - name: Setup xcodegen
        run: |
          if ! command -v xcodegen &> /dev/null; then
            brew install xcodegen
          fi
          xcodegen --version
      
      - name: Cache Java JRE
        id: cache-jre
        uses: actions/cache@v4
        with:
          path: |
            depends/java-*-openjdk
            depends/jre*.zip
            depends/jre*.tar.xz
          key: jre-ios-${{ hashFiles('Makefile') }}
      
      - name: Download Java JRE (via Makefile)
        timeout-minutes: 20
        run: |
          echo "=== 下载Java JRE for iOS arm64 (来源: assets.angelauramc.dev) ==="
          make jre || echo "WARNING: JRE download may have issues, continuing..."
        
      - name: Verify JRE Installation
        run: |
          echo "=== 验证JRE安装 ==="
          for jdir in depends/java-8-openjdk depends/java-17-openjdk depends/java-21-openjdk depends/java-25-openjdk; do
            if [ -f "$jdir/release" ]; then
              echo "✓ $jdir 已安装"
            else
              echo "✗ $jdir 缺失，重新下载..."
              make jre
              break
            fi
          done
      
      - name: Check Dependencies
        run: make check
      
      - name: Generate Xcode Project
        run: |
          xcodegen generate
          ls -la PCL-iOS.xcodeproj
      
      - name: Build App
        run: |
          xcodebuild -project PCL-iOS.xcodeproj \
            -scheme PCL-iOS \
            -configuration Debug \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            build 2>&1 | tee /tmp/build.log | tail -50
          if grep -q "BUILD FAILED" /tmp/build.log; then
            echo "=== BUILD FAILED ==="
            grep "error:" /tmp/build.log | head -20
            exit 1
          fi
      
      - name: Package IPA (不含JRE，避免ldid签名错误)
        run: |
          mkdir -p artifacts/Payload
          if [ -d "build/Build/Products/Debug-iphoneos/PCL-iOS.app" ]; then
            cp -r build/Build/Products/Debug-iphoneos/PCL-iOS.app artifacts/Payload/
          else
            echo "ERROR: Build product not found"
            find build -name "*.app" -type d 2>/dev/null
            exit 1
          fi
          # 不打包JRE到IPA中 - 参考Amethyst的slimmed版本
          # JRE将在首次启动时从网络下载
          cd artifacts
          zip -r ../PCL-iOS.ipa Payload
          cd ..
          ls -lh PCL-iOS.ipa
          echo "=== IPA内容 ==="
          unzip -l PCL-iOS.ipa | head -30
          
      - name: Package JRE as standalone artifact
        run: |
          echo "=== 打包JRE为独立压缩包 ==="
          mkdir -p jre_package
          if [ -d "depends/java-8-openjdk" ]; then
            cp -r depends/java-*-openjdk jre_package/
            cd jre_package
            zip -r ../PCL-iOS-JRE.zip java-*-openjdk
            cd ..
            ls -lh PCL-iOS-JRE.zip
          fi
          
      - name: Upload JRE
        uses: actions/upload-artifact@v4
        with:
          name: PCL-iOS-JRE.zip
          path: PCL-iOS-JRE.zip
          retention-days: 7
      
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: PCL-iOS.ipa
          path: PCL-iOS.ipa
          if-no-files-found: error
          retention-days: 7
